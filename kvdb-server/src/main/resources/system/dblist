# 是否激活数据库 ‘<name>’ ；如果为 true，则创建或加载该数据库并提供访问；
# ‘<name>’表示数据库名称，只允许用字母、数字、下划线组成；
db.test.enable=true

# 数据库 <name> 的根目录；如果未配置，则从默认目录加载(由 conf/kvdb.conf 指定了默认配置${DB_HOME}/../dbs/<name>)；
# db.test.rootdir=

# 数据库 <name> 的本地分区数；如果未配置，则采用 conf/kvdb.conf 中指定的默认配置
# db.test.partitions=

db.test2.enable=true
