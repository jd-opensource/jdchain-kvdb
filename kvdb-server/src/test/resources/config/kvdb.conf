# 数据库服务的本机监听地址；
server.host=0.0.0.0

# 数据库服务的本机监听端口；
server.port=7078

# 方式运行: cluster single
server.mode=single

# 数据库实例默认的根目录
dbs.rootdir=../dbs

# 数据库实例默认的本地分区数
dbs.partitions=4

# 数据库实例默认的rocksdb参数
# dbs.rocksdb.xxx=